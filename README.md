# CUDD 3.0.0 with A. Torralba changes for the Symbolic Planning Baseline (SymBA)

Use the following `configure` parameters to ensure a correct build
```
./configure --enable-obj --enable-shared --enable-dddmp
```

The original `README` can be found [here](README).